{
  "__inputs": [
    {
      "name": "DS_BETTERPVP",
      "label": "BetterPvP",
      "description": "BetterPvP PostgreSQL database",
      "type": "datasource",
      "pluginId": "postgres",
      "pluginName": "PostgreSQL"
    }
  ],
  "__elements": {},
  "__requires": [
    { "type": "grafana",    "id": "grafana",    "name": "Grafana",    "version": "9.0.0" },
    { "type": "datasource", "id": "postgres",   "name": "PostgreSQL", "version": "1.0.0" },
    { "type": "panel",      "id": "barchart",   "name": "Bar chart",  "version": "" },
    { "type": "panel",      "id": "table",      "name": "Table",      "version": "" }
  ],
  "annotations": { "list": [] },
  "description": "BetterPvP Champions Analytics — live queries directly against kills/champions_kills/client_stats. No schema changes required.",
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "refresh": "5m",
  "schemaVersion": 38,
  "tags": ["betterpvp", "champions"],
  "templating": {
    "list": [
      {
        "current": { "selected": false, "text": "6", "value": "6" },
        "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
        "definition": "SELECT DISTINCT realm FROM kills ORDER BY realm",
        "hide": 0,
        "includeAll": false,
        "label": "Realm",
        "multi": false,
        "name": "realm",
        "options": [],
        "query": "SELECT DISTINCT realm FROM kills ORDER BY realm",
        "refresh": 1,
        "regex": "",
        "sort": 1,
        "type": "query"
      }
    ]
  },
  "time": { "from": "now-5m", "to": "now" },
  "timepicker": { "hidden": true },
  "timezone": "browser",
  "title": "Champions Analytics (Live)",
  "uid": "betterpvp-champions-analytics",
  "version": 1,
  "weekStart": "",
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 10,
      "title": "Role Playtime",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
      "description": "Total hours played per role (all players aggregated), sorted by time descending. Also shows Kills, Deaths, and KDR per role.",
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": {
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "Hours Played",
            "axisPlacement": "auto",
            "fillOpacity": 80,
            "gradientMode": "none",
            "lineWidth": 1
          },
          "min": 0
        },
        "overrides": [
          {
            "matcher": { "id": "byName", "options": "KDR" },
            "properties": [
              { "id": "custom.axisPlacement", "value": "right" },
              { "id": "custom.axisLabel",     "value": "KDR" },
              { "id": "color", "value": { "fixedColor": "orange", "mode": "fixed" } },
              { "id": "custom.fillOpacity",   "value": 0 },
              { "id": "custom.lineWidth",     "value": 2 }
            ]
          },
          {
            "matcher": { "id": "byName", "options": "Kills" },
            "properties": [{ "id": "custom.hideFrom", "value": { "legend": true, "tooltip": false, "viz": true } }]
          },
          {
            "matcher": { "id": "byName", "options": "Deaths" },
            "properties": [{ "id": "custom.hideFrom", "value": { "legend": true, "tooltip": false, "viz": true } }]
          }
        ]
      },
      "gridPos": { "h": 12, "w": 12, "x": 0, "y": 1 },
      "id": 1,
      "options": {
        "barRadius": 0.05,
        "barWidth": 0.9,
        "groupWidth": 0.7,
        "legend": { "calcs": [], "displayMode": "list", "placement": "bottom", "showLegend": true },
        "orientation": "horizontal",
        "showValue": "auto",
        "stacking": "none",
        "tooltip": { "mode": "multi", "sort": "desc" },
        "xTickLabelRotation": 0,
        "xTickLabelSpacing": 200
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH role_time AS (\n    SELECT\n        StatData->'wrappedStat'->>'role' AS role,\n        SUM(Stat) AS time_played_ms\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_ROLE'\n      AND StatData->'wrappedStat'->>'action'   = 'TIME_PLAYED'\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'role'\n),\nrole_kills AS (\n    SELECT ck.killer_class AS role, COUNT(*) AS kills\n    FROM kills k\n    JOIN champions_kills ck ON k.id = ck.kill_id\n    WHERE k.realm = $realm AND k.valid = TRUE AND ck.killer_class <> ''\n    GROUP BY ck.killer_class\n),\nrole_deaths AS (\n    SELECT ck.victim_class AS role, COUNT(*) AS deaths\n    FROM kills k\n    JOIN champions_kills ck ON k.id = ck.kill_id\n    WHERE k.realm = $realm AND k.valid = TRUE AND ck.victim_class <> ''\n    GROUP BY ck.victim_class\n)\nSELECT\n    t.role AS \"Role\",\n    COALESCE(k.kills,  0) AS \"Kills\",\n    COALESCE(d.deaths, 0) AS \"Deaths\",\n    CASE\n        WHEN COALESCE(d.deaths, 0) = 0 THEN COALESCE(k.kills, 0)::NUMERIC\n        ELSE ROUND(COALESCE(k.kills, 0)::NUMERIC / d.deaths, 2)\n    END AS \"KDR\",\n    ROUND(t.time_played_ms / 3600000.0, 2) AS \"Hours Played\"\nFROM role_time t\nLEFT JOIN role_kills  k ON t.role = k.role\nLEFT JOIN role_deaths d ON t.role = d.role\nORDER BY t.time_played_ms DESC",
          "refId": "A"
        }
      ],
      "title": "Role Playtime (Hours)",
      "type": "barchart"
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
      "description": "Kills, Deaths, Hours Played and KDR per role in a sortable table.",
      "fieldConfig": {
        "defaults": { "custom": { "align": "auto", "cellOptions": { "type": "auto" } } },
        "overrides": [
          {
            "matcher": { "id": "byName", "options": "KDR" },
            "properties": [
              {
                "id": "thresholds",
                "value": { "mode": "absolute", "steps": [
                  { "color": "red",    "value": null },
                  { "color": "yellow", "value": 0.8 },
                  { "color": "green",  "value": 1.0 }
                ]}
              },
              { "id": "custom.cellOptions", "value": { "type": "color-background" } }
            ]
          },
          {
            "matcher": { "id": "byName", "options": "Hours Played" },
            "properties": [{ "id": "custom.width", "value": 130 }]
          }
        ]
      },
      "gridPos": { "h": 12, "w": 12, "x": 12, "y": 1 },
      "id": 2,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "show": false },
        "showHeader": true,
        "sortBy": [{ "desc": true, "displayName": "Hours Played" }]
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH role_time AS (\n    SELECT\n        StatData->'wrappedStat'->>'role' AS role,\n        SUM(Stat) AS time_played_ms\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_ROLE'\n      AND StatData->'wrappedStat'->>'action'   = 'TIME_PLAYED'\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'role'\n),\nrole_kills AS (\n    SELECT ck.killer_class AS role, COUNT(*) AS kills\n    FROM kills k\n    JOIN champions_kills ck ON k.id = ck.kill_id\n    WHERE k.realm = $realm AND k.valid = TRUE AND ck.killer_class <> ''\n    GROUP BY ck.killer_class\n),\nrole_deaths AS (\n    SELECT ck.victim_class AS role, COUNT(*) AS deaths\n    FROM kills k\n    JOIN champions_kills ck ON k.id = ck.kill_id\n    WHERE k.realm = $realm AND k.valid = TRUE AND ck.victim_class <> ''\n    GROUP BY ck.victim_class\n)\nSELECT\n    t.role AS \"Role\",\n    COALESCE(k.kills,  0) AS \"Kills\",\n    COALESCE(d.deaths, 0) AS \"Deaths\",\n    CASE\n        WHEN COALESCE(d.deaths, 0) = 0 THEN COALESCE(k.kills, 0)::NUMERIC\n        ELSE ROUND(COALESCE(k.kills, 0)::NUMERIC / d.deaths, 2)\n    END AS \"KDR\",\n    ROUND(t.time_played_ms / 3600000.0, 2) AS \"Hours Played\"\nFROM role_time t\nLEFT JOIN role_kills  k ON t.role = k.role\nLEFT JOIN role_deaths d ON t.role = d.role\nORDER BY t.time_played_ms DESC",
          "refId": "A"
        }
      ],
      "title": "Role Playtime Table",
      "type": "table"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 13 },
      "id": 11,
      "title": "Role KDR",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
      "description": "KDR per role, sorted descending. Colours: green ≥ 1.0, yellow ≥ 0.8, red < 0.8.",
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "custom": {
            "axisLabel": "KDR",
            "fillOpacity": 80,
            "lineWidth": 1
          },
          "min": 0,
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red",    "value": null },
              { "color": "yellow", "value": 0.8 },
              { "color": "green",  "value": 1.0 }
            ]
          }
        },
        "overrides": []
      },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 14 },
      "id": 3,
      "options": {
        "barRadius": 0.05,
        "barWidth": 0.9,
        "groupWidth": 0.7,
        "legend": { "calcs": [], "displayMode": "list", "placement": "bottom", "showLegend": false },
        "orientation": "horizontal",
        "showValue": "auto",
        "stacking": "none",
        "tooltip": { "mode": "single", "sort": "none" },
        "xTickLabelRotation": 0
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH role_time AS (\n    SELECT StatData->'wrappedStat'->>'role' AS role, SUM(Stat) AS time_played_ms\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_ROLE'\n      AND StatData->'wrappedStat'->>'action'   = 'TIME_PLAYED'\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'role'\n),\nrole_kills AS (\n    SELECT ck.killer_class AS role, COUNT(*) AS kills\n    FROM kills k JOIN champions_kills ck ON k.id = ck.kill_id\n    WHERE k.realm = $realm AND k.valid = TRUE AND ck.killer_class <> ''\n    GROUP BY ck.killer_class\n),\nrole_deaths AS (\n    SELECT ck.victim_class AS role, COUNT(*) AS deaths\n    FROM kills k JOIN champions_kills ck ON k.id = ck.kill_id\n    WHERE k.realm = $realm AND k.valid = TRUE AND ck.victim_class <> ''\n    GROUP BY ck.victim_class\n)\nSELECT\n    t.role AS \"Role\",\n    CASE\n        WHEN COALESCE(d.deaths, 0) = 0 THEN COALESCE(k.kills, 0)::NUMERIC\n        ELSE ROUND(COALESCE(k.kills, 0)::NUMERIC / d.deaths, 2)\n    END AS \"KDR\"\nFROM role_time t\nLEFT JOIN role_kills  k ON t.role = k.role\nLEFT JOIN role_deaths d ON t.role = d.role\nORDER BY \"KDR\" DESC",
          "refId": "A"
        }
      ],
      "title": "Role KDR",
      "type": "barchart"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 24 },
      "id": 12,
      "title": "Role Matchup KDR",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
      "description": "KDR for every class matchup. Each row = <Role> attacking <vs Role>.\n\nKDR column is colour-coded: green ≥ 1.0, yellow ≥ 0.8, red < 0.8.\nSort any column by clicking the header.",
      "fieldConfig": {
        "defaults": { "custom": { "align": "auto", "cellOptions": { "type": "auto" } } },
        "overrides": [
          {
            "matcher": { "id": "byName", "options": "KDR" },
            "properties": [
              {
                "id": "thresholds",
                "value": { "mode": "absolute", "steps": [
                  { "color": "red",    "value": null },
                  { "color": "yellow", "value": 0.8 },
                  { "color": "green",  "value": 1.0 }
                ]}
              },
              { "id": "custom.cellOptions", "value": { "type": "color-background" } },
              { "id": "custom.width",       "value": 80 }
            ]
          },
          {
            "matcher": { "id": "byName", "options": "Kills" },
            "properties": [{ "id": "custom.width", "value": 80 }]
          },
          {
            "matcher": { "id": "byName", "options": "Deaths" },
            "properties": [{ "id": "custom.width", "value": 80 }]
          }
        ]
      },
      "gridPos": { "h": 18, "w": 24, "x": 0, "y": 25 },
      "id": 4,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "show": false },
        "showHeader": true,
        "sortBy": [{ "desc": false, "displayName": "Role" }]
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH matchup_kills AS (\n    SELECT\n        ck.killer_class AS attacker,\n        ck.victim_class AS defender,\n        COUNT(*) AS kills\n    FROM kills k\n    JOIN champions_kills ck ON k.id = ck.kill_id\n    WHERE k.realm = $realm\n      AND k.valid = TRUE\n      AND ck.killer_class <> ''\n      AND ck.victim_class <> ''\n    GROUP BY ck.killer_class, ck.victim_class\n)\nSELECT\n    a.attacker            AS \"Role\",\n    a.defender            AS \"vs Role\",\n    a.kills               AS \"Kills\",\n    COALESCE(b.kills, 0)  AS \"Deaths\",\n    CASE\n        WHEN COALESCE(b.kills, 0) = 0 THEN a.kills::NUMERIC\n        ELSE ROUND(a.kills::NUMERIC / b.kills, 2)\n    END                   AS \"KDR\"\nFROM matchup_kills a\nLEFT JOIN matchup_kills b\n       ON a.attacker = b.defender\n      AND a.defender = b.attacker\nORDER BY a.attacker, \"KDR\" DESC",
          "refId": "A"
        }
      ],
      "title": "Role Matchup KDR",
      "type": "table"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 43 },
      "id": 13,
      "title": "Skill KDR",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
      "description": "KDR per skill (all levels aggregated), sorted descending. Shows hours played per skill.",
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": {
            "axisLabel": "KDR",
            "fillOpacity": 80,
            "lineWidth": 1
          },
          "min": 0
        },
        "overrides": []
      },
      "gridPos": { "h": 14, "w": 24, "x": 0, "y": 44 },
      "id": 5,
      "options": {
        "barRadius": 0.05,
        "barWidth": 0.85,
        "groupWidth": 0.7,
        "legend": { "calcs": [], "displayMode": "list", "placement": "bottom", "showLegend": false },
        "orientation": "vertical",
        "showValue": "never",
        "stacking": "none",
        "tooltip": { "mode": "single", "sort": "none" },
        "xTickLabelRotation": -45,
        "xTickLabelSpacing": 0
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH skill_kills AS (\n    SELECT\n        StatData->'wrappedStat'->>'skillName' AS skill_name,\n        SUM(Stat) AS kills\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_SKILL'\n      AND StatData->'wrappedStat'->>'action'   = 'KILL'\n      AND StatData->'wrappedStat'->>'skillName' IS NOT NULL\n      AND StatData->'wrappedStat'->>'skillName' != ''\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'skillName'\n),\nskill_deaths AS (\n    SELECT\n        StatData->'wrappedStat'->>'skillName' AS skill_name,\n        SUM(Stat) AS deaths\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_SKILL'\n      AND StatData->'wrappedStat'->>'action'   = 'DEATH'\n      AND StatData->'wrappedStat'->>'skillName' IS NOT NULL\n      AND StatData->'wrappedStat'->>'skillName' != ''\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'skillName'\n),\nskill_time AS (\n    SELECT\n        StatData->'wrappedStat'->>'skillName' AS skill_name,\n        SUM(Stat) AS time_played_ms\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_SKILL'\n      AND StatData->'wrappedStat'->>'action'   = 'TIME_PLAYED'\n      AND StatData->'wrappedStat'->>'skillName' IS NOT NULL\n      AND StatData->'wrappedStat'->>'skillName' != ''\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'skillName'\n)\nSELECT\n    k.skill_name AS \"Skill\",\n    k.kills AS \"Kills\",\n    COALESCE(d.deaths, 0) AS \"Deaths\",\n    CASE\n        WHEN COALESCE(d.deaths, 0) = 0 THEN k.kills::NUMERIC\n        ELSE ROUND(k.kills::NUMERIC / d.deaths, 2)\n    END AS \"KDR\",\n    ROUND(COALESCE(t.time_played_ms, 0) / 3600000.0, 2) AS \"Hours Played\"\nFROM skill_kills k\nLEFT JOIN skill_deaths d ON k.skill_name = d.skill_name\nLEFT JOIN skill_time   t ON k.skill_name = t.skill_name\nORDER BY \"KDR\" DESC",
          "refId": "A"
        }
      ],
      "title": "Skill KDR",
      "type": "barchart"
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
      "description": "Skill KDR, Kills, Deaths, and Hours Played in a sortable table.",
      "fieldConfig": {
        "defaults": { "custom": { "align": "auto", "cellOptions": { "type": "auto" } } },
        "overrides": [
          {
            "matcher": { "id": "byName", "options": "KDR" },
            "properties": [
              {
                "id": "thresholds",
                "value": { "mode": "absolute", "steps": [
                  { "color": "red",    "value": null },
                  { "color": "yellow", "value": 0.8 },
                  { "color": "green",  "value": 1.0 }
                ]}
              },
              { "id": "custom.cellOptions", "value": { "type": "color-background" } },
              { "id": "custom.width",       "value": 80 }
            ]
          }
        ]
      },
      "gridPos": { "h": 14, "w": 24, "x": 0, "y": 58 },
      "id": 6,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "show": false },
        "showHeader": true,
        "sortBy": [{ "desc": true, "displayName": "KDR" }]
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${DS_BETTERPVP}" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH skill_kills AS (\n    SELECT\n        StatData->'wrappedStat'->>'skillName' AS skill_name,\n        SUM(Stat) AS kills\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_SKILL'\n      AND StatData->'wrappedStat'->>'action'   = 'KILL'\n      AND StatData->'wrappedStat'->>'skillName' IS NOT NULL\n      AND StatData->'wrappedStat'->>'skillName' != ''\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'skillName'\n),\nskill_deaths AS (\n    SELECT\n        StatData->'wrappedStat'->>'skillName' AS skill_name,\n        SUM(Stat) AS deaths\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_SKILL'\n      AND StatData->'wrappedStat'->>'action'   = 'DEATH'\n      AND StatData->'wrappedStat'->>'skillName' IS NOT NULL\n      AND StatData->'wrappedStat'->>'skillName' != ''\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'skillName'\n),\nskill_time AS (\n    SELECT\n        StatData->'wrappedStat'->>'skillName' AS skill_name,\n        SUM(Stat) AS time_played_ms\n    FROM client_stats\n    WHERE StatType = 'CLANS_WRAPPER'\n      AND StatData->'wrappedStat'->>'statType' = 'CHAMPIONS_SKILL'\n      AND StatData->'wrappedStat'->>'action'   = 'TIME_PLAYED'\n      AND StatData->'wrappedStat'->>'skillName' IS NOT NULL\n      AND StatData->'wrappedStat'->>'skillName' != ''\n      AND Realm = $realm\n    GROUP BY StatData->'wrappedStat'->>'skillName'\n)\nSELECT\n    k.skill_name AS \"Skill\",\n    k.kills AS \"Kills\",\n    COALESCE(d.deaths, 0) AS \"Deaths\",\n    CASE\n        WHEN COALESCE(d.deaths, 0) = 0 THEN k.kills::NUMERIC\n        ELSE ROUND(k.kills::NUMERIC / d.deaths, 2)\n    END AS \"KDR\",\n    ROUND(COALESCE(t.time_played_ms, 0) / 3600000.0, 2) AS \"Hours Played\"\nFROM skill_kills k\nLEFT JOIN skill_deaths d ON k.skill_name = d.skill_name\nLEFT JOIN skill_time   t ON k.skill_name = t.skill_name\nORDER BY \"KDR\" DESC",
          "refId": "A"
        }
      ],
      "title": "Skill KDR Table",
      "type": "table"
    }
  ]
}
