name: Lunar
version: '1.0'
main: me.mykindos.betterpvp.lunar.Lunar
api-version: '1.21'

dependencies:
  bootstrap:
    Core:
      load: BEFORE
      required: true
      join-classpath: true
  server:
    Core:
      load: BEFORE
      required: true
      join-classpath: true