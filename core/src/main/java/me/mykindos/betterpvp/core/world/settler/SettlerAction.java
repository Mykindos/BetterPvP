package me.mykindos.betterpvp.core.world.settler;

/** Something a player can do to a site's settlers, each checked against the owner's permissions. */
public enum SettlerAction {
    HIRE,
    ASSIGN,
    DISMISS,
    PAY
}
