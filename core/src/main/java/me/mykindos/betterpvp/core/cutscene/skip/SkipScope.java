package me.mykindos.betterpvp.core.cutscene.skip;

/** How much of a cutscene a skip would consume. */
public enum SkipScope {

    /** Advance past the current beat only. */
    BEAT,

    /** End the whole cutscene. */
    CUTSCENE
}
